instances:
  - name: es
    dns:
      - "{{ lookup(name) }}"
    ip:
      - 192.168.2.21

  - name: es
    dns:
      - "{{ lookup(name) }}"
    ip:
      - 192.168.3.22

  - name: es
    dns:
      - "{{ lookup(name) }}"
    ip:
      - 192.168.2.23